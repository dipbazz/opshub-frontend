# Copy to .env and fill in. .env is gitignored — never commit real values.

# Local dev: http://localhost:8000 (backend running via docker compose)
# Prod (Vercel): the DigitalOcean droplet's public URL, set in Vercel's
# project environment variables, not committed here.
VITE_API_URL=http://localhost:8000
